generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

enum UserRole {
  OWNER
  ADMIN
  MANAGER
  TENANT
}

enum PropertyStatus {
  ACTIVE
  SOLD
  INACTIVE
}

enum UnitStatus {
  AVAILABLE
  RESERVED
  RENTED
  SOLD
}

enum ContractStatus {
  DRAFT
  SENT
  SIGNED
  TERMINATED
}

enum MaintenanceStatus {
  REPORTED
  IN_PROGRESS
  COMPLETED
}

enum InspectionType {
  MOVE_IN
  MOVE_OUT
}

model User {
  id        String   @id @default(uuid())
  authId    String?  @unique // Link to Supabase Auth ID
  name      String
  email     String   @unique
  phone     String?
  address   String?
  postalCode String?
  city      String?
  hasTenantCertificate Boolean @default(false)
  role      UserRole @default(TENANT)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  // Relations
  properties          Property[]            @relation("OwnerProperties")
  leaseContracts      LeaseContract[]
  maintenanceRequests MaintenanceRequest[]
  messages            Message[]
  interests           Interest[]
  
  // Tenant Certificate relations
  receivedCertificates TenantCertificate[] @relation("ReceivedCertificates")
  issuedCertificates   TenantCertificate[] @relation("IssuedCertificates")
  
  // For Fiken integration (only for owners)
  fikenCompanySlug    String?
  fikenApiToken       String?

  // Settings
  standardRentPerSqm  Int      @default(185)
}

model TenantCertificate {
  id            String   @id @default(uuid())
  tenantId      String
  issuerId      String
  
  totalScore    Int      // 1-10
  behaviorScore Int      // Oppførsel
  noiseScore    Int      // Lydnivå
  paymentScore  Int      // Leie betalt som avtalt
  cleaningScore Int      // Utvask og tilbakelevering
  
  comment       String?
  createdAt     DateTime @default(now())
  
  tenant        User     @relation("ReceivedCertificates", fields: [tenantId], references: [id])
  issuer        User     @relation("IssuedCertificates", fields: [issuerId], references: [id])
}

model Property {
  id        String         @id @default(uuid())
  name      String
  address   String
  gnr       String?
  bnr       String?
  snr       String?
  parentId  String?
  ownerId   String
  status    PropertyStatus @default(ACTIVE)
  notes     String?
  imageUrl  String?
  createdAt DateTime       @default(now())
  updatedAt DateTime       @updatedAt

  owner     User           @relation("OwnerProperties", fields: [ownerId], references: [id])
  parent    Property?      @relation("PropertyHierarchy", fields: [parentId], references: [id])
  children  Property[]     @relation("PropertyHierarchy")
  units     Unit[]
}

model Unit {
  id             String      @id @default(uuid())
  propertyId     String
  name           String      // e.g. "Apt 101"
  unitNumber     String?     // e.g. "H0101"
  sizeSqm        Float
  roomCount      Int
  rentAmount     Float
  depositAmount  Float
  status         UnitStatus  @default(AVAILABLE)
  notes          String?
  imageUrl       String?
  createdAt      DateTime    @default(now())
  updatedAt      DateTime    @updatedAt

  property       Property    @relation(fields: [propertyId], references: [id], onDelete: Cascade)
  leaseContracts LeaseContract[]
  maintenanceRequests MaintenanceRequest[]
  rooms      Room[]
  interests  Interest[]
}

enum RoomType {
  LIVING_ROOM
  KITCHEN
  BEDROOM
  BATHROOM
  HALLWAY
  STORAGE
  BALCONY
  GARAGE
  OTHER
}

model Room {
  id          String   @id @default(uuid())
  unitId      String
  name        String   // e.g. "Stue", "Kjøkken"
  type        RoomType @default(OTHER)
  sizeSqm     Float?
  description String?

  // 3D Scan info
  scanUrl     String?  // URL to .glb/.usdz file
  scanFormat  String?  // "GLB", "USDZ", "OBJ"
  scanStatus  String   @default("PENDING") // "PENDING", "PROCESSING", "COMPLETED", "FAILED"
  thumbnailUrl String?
  
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  unit        Unit     @relation(fields: [unitId], references: [id], onDelete: Cascade)
}

model LeaseContract {
  id             String         @id @default(uuid())
  unitId         String
  tenantId       String
  startDate      DateTime
  endDate        DateTime?
  rentAmount     Float
  depositAmount  Float
  status         ContractStatus @default(DRAFT)
  signedAt       DateTime?
  signedIp       String?
  pdfUrl         String?        // URL to stored PDF
  createdAt      DateTime       @default(now())
  updatedAt      DateTime       @updatedAt

  // Fiken integration
  fikenCustomerId String?
  fikenInvoiceId  String?

  unit           Unit           @relation(fields: [unitId], references: [id], onDelete: Cascade)
  tenant         User           @relation(fields: [tenantId], references: [id])
  messageThread  MessageThread?
  inspectionProtocols InspectionProtocol[]
}

model MessageThread {
  id         String    @id @default(uuid())
  contractId String    @unique
  createdAt  DateTime  @default(now())

  contract   LeaseContract @relation(fields: [contractId], references: [id], onDelete: Cascade)
  messages   Message[]
}

model Message {
  id        String   @id @default(uuid())
  threadId  String
  senderId  String
  content   String
  timestamp DateTime @default(now())

  thread    MessageThread @relation(fields: [threadId], references: [id], onDelete: Cascade)
  sender    User          @relation(fields: [senderId], references: [id])
}

model MaintenanceRequest {
  id          String            @id @default(uuid())
  unitId      String
  tenantId    String
  title       String
  description String
  status      MaintenanceStatus @default(REPORTED)
  createdAt   DateTime          @default(now())
  updatedAt   DateTime          @updatedAt

  unit        Unit              @relation(fields: [unitId], references: [id], onDelete: Cascade)
  tenant      User              @relation(fields: [tenantId], references: [id])
}

model InspectionProtocol {
  id             String         @id @default(uuid())
  contractId     String
  type           InspectionType
  notes          String?
  createdAt      DateTime       @default(now())
  signedByTenant Boolean        @default(false)
  signedByOwner  Boolean        @default(false)

  checkpoints    InspectionCheckpoint[]
  contract       LeaseContract  @relation(fields: [contractId], references: [id], onDelete: Cascade)
}

model InspectionCheckpoint {
  id              String             @id @default(uuid())
  protocolId      String
  room            String
  element         String
  status          String             // e.g. "OK", "Mangel", "Ikke vurdert"
  notes           String?
  
  protocol        InspectionProtocol @relation(fields: [protocolId], references: [id], onDelete: Cascade)
}

model Interest {
  id        String         @id @default(uuid())
  unitId    String
  userId    String?        // Optional: if they are logged in
  name      String
  email     String
  phone     String?
  message   String?
  status    InterestStatus @default(PENDING)
  createdAt DateTime       @default(now())
  updatedAt DateTime       @updatedAt

  unit      Unit     @relation(fields: [unitId], references: [id], onDelete: Cascade)
  user      User?    @relation(fields: [userId], references: [id])
}

enum InterestStatus {
  PENDING
  CONTACTED
  REJECTED
  OFFERED
}
